import { CardElement } from "./styles/card"

// eslint-disable-next-line react/prop-types
export default function Card({events}) {
    // eslint-disable-next-line react/prop-types
    console.log(events.author)
    return (
        <CardElement>
            
        </CardElement>
    )
}